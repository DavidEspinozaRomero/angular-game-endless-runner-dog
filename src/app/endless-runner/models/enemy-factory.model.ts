import { Game } from './game.model';

// enemies Factory
// export class RandomEnemyFactory {
//   // type: 'FlyingEnemy' | 'GroundEnemy' | 'ClimbingEnemy',
//   static createEnemy(game: Game) {
//     const typeEnemy = Math.random();

//     if (typeEnemy < 0.3) return new FlyingEnemy(game);
//     if (typeEnemy < 0.6) return new GroundEnemy(game);
//     return new ClimbingEnemy(game);
//   }
// }
